import type { Core } from './types';
import { type Logger } from '@applitools/logger';
type Options = {
    agentId?: string;
    concurrency?: number;
    logger?: Logger;
    maskLog?: boolean;
    offlineLocationPath?: string;
};
export declare function makeCore({ agentId, concurrency, maskLog, logger: defaultLogger, offlineLocationPath, }: Options): Core;
export {};
